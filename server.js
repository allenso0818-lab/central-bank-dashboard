const http=require("http"),fs=require("fs"),path=require("path");const port=process.env.PORT||3000;http.createServer((req,res)=>{let p=req.url==="/"?"index.html":req.url.replace(/^\//,"");let f=path.join(__dirname,p);fs.readFile(f,(e,d)=>{if(e){res.writeHead(404);return res.end("Not found")}let ext=path.extname(f);let type=ext===".html"?"text/html; charset=utf-8":ext===".js"?"text/javascript; charset=utf-8":"application/octet-stream";res.writeHead(200,{"Content-Type":type});res.end(d)})}).listen(port,"0.0.0.0",()=>console.log("Dashboard running on "+port));